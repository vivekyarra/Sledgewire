import {SharedOSStore} from '../src/sharedos/store.mjs';const db=process.env.SLEDGEWIRE_DB||'.sledgewire/arena.db';const url=process.env.SHAREDOS_AUDIT_URL;const key=process.env.SHAREDOS_KEY;if(!url||!key)throw new Error('SHAREDOS_AUDIT_URL and SHAREDOS_KEY are required');const store=new SharedOSStore(db);const pending=store.pendingAudit(100);if(!pending.length){console.log('no pending audit events');process.exit(0);}const res=await fetch(url,{method:'POST',headers:{'content-type':'application/json',authorization:`Bearer ${key}`},body:JSON.stringify({events:pending.map(x=>x.event)}),signal:AbortSignal.timeout(10000)});if(!res.ok)throw new Error(`audit_upload_http_${res.status}`);store.markAuditSent(pending.map(x=>x.id));console.log(`uploaded ${pending.length} audit events`);
