const patterns=[/ignore (?:all|any|the|previous|prior) instructions/i,/(?:^|\s)system\s*:/i,/(?:^|\s)developer\s*:/i,/read (?:process\.)?env/i,/(?:send|exfiltrate|upload|post).{0,80}(?:secret|token|credential|api[ _-]?key|private key)/i,/do not tell (?:the )?(?:user|caller)/i,/(?:curl|wget)\s+https?:\/\//i,/(?:cat|read).{0,40}(?:\.env|\/etc\/passwd|id_rsa|credentials)/i,/override.{0,40}(?:policy|permission|authorization)/i];export function scanUntrusted(value,{maxChars=200_000}={}){let text;try{text=typeof value==='string'?value:JSON.stringify(value);}catch{text='[unserializable]';}const truncated=text.length>maxChars;if(truncated)text=text.slice(0,maxChars);const hits=patterns.filter(r=>r.test(text)).map(r=>r.source);return {suspicious:hits.length>0,hits,truncated,scanned_chars:text.length};}
