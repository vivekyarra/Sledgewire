import {sha256} from '../receipts/receipt.mjs';
export function paymentMemo(requestId,service){return `sledgewire:${requestId}:${service}`;}
export function requestFingerprint({roomId,buyerSeat,requestId,service,input}){return sha256({roomId,buyerSeat,requestId,service,input});}
export class PaymentGate{
  constructor({ledger,store,prices,payee}){this.ledger=ledger;this.store=store;this.prices=prices;this.payee=payee;}
  async authorize(req,signal){const price=this.prices[req.service];if(!Number.isInteger(price)||price<=0)return {ok:false,reason:'unknown_or_free_service'};if(!req.txnId)return {ok:false,reason:'payment_required',price,memo:paymentMemo(req.requestId,req.service)};const tx=await this.ledger.get(req.txnId,signal);if(!tx)return {ok:false,reason:'transaction_not_found'};const memo=paymentMemo(req.requestId,req.service);if(tx.by_instance_id!==req.buyerSeat)return {ok:false,reason:'wrong_buyer'};if((tx.addressed_to??tx.to)!==this.payee)return {ok:false,reason:'wrong_payee'};if(Number(tx.amount)!==price)return {ok:false,reason:'wrong_amount'};if(tx.room_id!==req.roomId)return {ok:false,reason:'wrong_room'};if(tx.memo!==memo)return {ok:false,reason:'wrong_memo'};const fp=requestFingerprint(req);const claim=this.store.claim({requestId:req.requestId,txnId:req.txnId,fingerprint:fp,service:req.service});if(claim.status==='conflict')return {ok:false,reason:'transaction_or_request_reused'};if(claim.status==='inflight')return {ok:false,reason:'request_already_inflight',fingerprint:fp};if(claim.status==='failed')return {ok:false,reason:'previous_attempt_failed',fingerprint:fp,error:claim.error};if(claim.status==='replay')return {ok:true,replay:true,fingerprint:fp,cached:claim.response,price};return {ok:true,replay:false,fingerprint:fp,price};}
}
