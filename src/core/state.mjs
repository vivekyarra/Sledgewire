export const STATES=Object.freeze({READY:'READY',DEGRADED:'DEGRADED',INCOMPATIBLE:'INCOMPATIBLE',BLOCKED:'BLOCKED',UNKNOWN:'UNKNOWN'});
export function summarize(checks){const vals=Object.values(checks).map(x=>x?.status);if(vals.includes('security_block'))return STATES.BLOCKED;if(vals.includes('fail'))return STATES.INCOMPATIBLE;if(vals.includes('unknown'))return STATES.UNKNOWN;if(vals.includes('warn'))return STATES.DEGRADED;return STATES.READY;}
