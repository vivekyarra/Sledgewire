import readline from 'node:readline';import {handleRpc} from './protocol.mjs';const rl=readline.createInterface({input:process.stdin,crlfDelay:Infinity});for await(const line of rl){if(!line.trim())continue;let msg;try{msg=JSON.parse(line);}catch{console.log(JSON.stringify({jsonrpc:'2.0',id:null,error:{code:-32700,message:'Parse error'}}));continue;}const out=await handleRpc(msg);if(out!==null)console.log(JSON.stringify(out));}
