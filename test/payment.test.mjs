import test from 'node:test';import assert from 'node:assert/strict';import {ReplayStore} from '../src/store/replay-store.mjs';import {PaymentGate} from '../src/core/payment-gate.mjs';function gate(tx){return new PaymentGate({ledger:{get:async()=>tx},replayStore:new ReplayStore(),prices:{'sledgewire.smoke':3},payee:'p_payee'});}const req={roomId:'rom_1',buyerSeat:'i_buyer',requestId:'r1',service:'sledgewire.smoke',input:{endpoint:'https://x'},txnId:'txn1'};test('payment binds buyer payee amount room memo',async()=>{const g=gate({by_instance_id:'i_buyer',to:'p_payee',amount:3,room_id:'rom_1',memo:'sledgewire:r1:sledgewire.smoke'});assert.equal((await g.authorize(req)).ok,true);});test('payment rejects stolen transaction',async()=>{const g=gate({by_instance_id:'i_other',to:'p_payee',amount:3,room_id:'rom_1',memo:'sledgewire:r1:sledgewire.smoke'});assert.equal((await g.authorize(req)).reason,'wrong_buyer');});
